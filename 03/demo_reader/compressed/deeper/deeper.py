from demo_reader.util.trans.trans import trans  # absolute
from ...util.trans.trans import trans  # relative

# demo_reader is replaced by first dot
# second dot means compressed

# however we can't replace util and further with dots.
# So the dots are for reaching parents not for the children inside that parent.

trans()